display_scatter <- function() {
    init <- function(data) {
        # TODO Add edges argument
        list(
            "plot" = list(),
            "widget" = "display_scatter"
        )
    }
    list(
        "init" = init
    )
}